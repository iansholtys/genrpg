{
  "name": "genrpg",
  "version": "0.1.0",
  "private": true,
  "description": "GenRPG web application",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "db:apply": "node src/db/versions.js",
    "build:css": "sass public/scss/styles.scss public/styles.css --style=expanded --no-source-map",
    "watch:css": "sass public/scss/styles.scss public/styles.css --watch --no-source-map",
    "check": "node --check src/server.js && node --check src/db/pool.js && node --check src/db/versions.js && node --check src/packages.js && node --check src/updates.js"
  },
  "dependencies": {
    "connect-pg-simple": "9.0.1",
    "express": "^4.22.2",
    "express-session": "^1.19.0",
    "openid-client": "5.7.1",
    "pg": "8.12.0",
    "semver": "^7.7.2",
    "yaml": "^2.8.0"
  },
  "devDependencies": {
    "sass": "^1.89.0"
  },
  "engines": {
    "node": ">=20"
  }
}
